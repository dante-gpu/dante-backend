[alembic]
# a relative path to process for migrations
script_location = alembic

# Logging configuration
# ... (logging config unchanged)

# sqlalchemy.url = driver://user:pass@localhost/dbname
# I should comment this out, as the URL is now provided by env.py from app settings.
s sqlalchemy.url = 

# ... other alembic settings ... 